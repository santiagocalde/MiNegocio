# Map to detect webshell probe patterns in URI
map $uri $webshell_probe {
    default 0;
    ~*(wso|bless|lock360|zwso|f35|mini|simple|ahax|amax|chosen|inputs|goods|file5|otuz1|O-Simple|mah|robots|core)\.php 1;
}
