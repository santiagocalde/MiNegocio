# Dependencias solo para desarrollo y tests (NO se instalan en la imagen de prod).
# Uso:  pip install -r requirements.txt -r requirements-dev.txt
#       pytest
#
# Los tests de integración PG (tests/test_pg_integration.py) necesitan un
# PostgreSQL accesible (variables PG_HOST/PG_PORT/PG_USER/PG_PASSWORD). Si no lo
# hay, se saltean solos y la suite SQLite sigue corriendo.
-r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0
